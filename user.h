
#include<stdio.h>
#include "list.h"


void userMode(void);
int student_Search1(struct student*arr,char*id);

void student_editPassword(struct student*arr,char*password );
void student_editName(struct student*arr,char*name );
